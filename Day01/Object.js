//Array Object 
/*const person = {
    firstName: "John",
    lastName: "Doe",
    age: 50,
    eyeColor: "blue"
}
console.log(person);
//Here propert is firstName, lastName, age, eyecolor and the value is john, doe, 50, blue


//Accessing the object properties
const person={
    firstName:"John",
    lastName: "Parker"
}
let name= person.firstName; //objectName.propertyName
console.log(name); 

//Object Methods : It is a function stored as a property
//methods are actions that can be performed on objects
//It is stored in properties as function definitions

const person = {
    firstName: "John",
    lastName : "Doe",
    id       : 5566,
    fullName : function() {
      return this.firstName + " " + this.lastName;
    }
  };

console.log(person.fullName());

//this keyword refers to an object. Above the object is person
//if it use like : this.firstname it means it is referring to the firstname of the person object
//this is a global object

//We cannot declare String , numbers and booleans as object
*/

//Object.keys() methods returns an Array Iterator object with the keys of an object.


