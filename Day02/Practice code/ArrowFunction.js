//Arrow Function
//  Arrow function  allows us to write shorter function syntax
// In arrow function we can declare a variables to store a function
// If the function has only one statement and the statement returns a value, we can remove the brackets and return keyword
// Handling of this keyword is different. Here this keywors represent the object that is defined the arrow function

//Example
/*
this.ObjectName='Srashta';
console.log(this);

const test = {

    //Regular Function
    func1: function(){
        console.log('func1', this); //it calls the test object and it console log the functions inside the object

    },

    //Arrow Function
    func2: () => {
        console.log('func2', this); // it calls the only the function within the arrow function
    }

};

test.func1();
test.func2();      */
